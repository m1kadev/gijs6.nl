#!/bin/bash

serve() {
  .venv/bin/python main.py
}

deploy() {
  ssh pya "chmod +x /home/gijs3/web/deploy.sh && /home/gijs3/web/deploy.sh"
}

force_deploy() {
  deploy
  source .env
  curl -X POST "https://eu.pythonanywhere.com/api/v0/user/gijs3/webapps/www.gijs6.nl/reload/" -H "Authorization: Token $API_TOKEN"
}

format() {
  ruff format .
  prettier --write "**/*.{css,js}"
  prettier --write "**/*.json"
  djlint . --reformat
  markdownlint --fix "**/*.md" || true
}
